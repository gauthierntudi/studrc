# Secrets & env
.env
.env.*
!.env.example
deploy/.env
**/credentials*
**/*secret*

# Dependencies & build
node_modules/
dist/
.next/
out/
coverage/
*.tsbuildinfo

# Prisma
apps/api/prisma/*.db
apps/api/prisma/*.db-journal

# Compose override VPS (Postgres/Redis locaux, réseaux) — hors git
docker-compose.override.yml
docker-compose.override.yaml

# Certs
deploy/certbot/

# Vhosts nginx locaux au VPS (Chrononews, etc.) — garder example + placeholder
deploy/nginx/local/*.conf
!deploy/nginx/local/_placeholder.conf

# OS / IDE
.DS_Store
*.log
.idea/
.vscode/
