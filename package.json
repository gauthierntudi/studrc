{
  "name": "studrc",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "pnpm -r --parallel --filter @studrc/api --filter @studrc/web dev",
    "dev:up": "pnpm docker:dev && pnpm dev",
    "dev:api": "pnpm --filter @studrc/api dev",
    "dev:web": "pnpm --filter @studrc/web dev",
    "build": "pnpm -r --filter @studrc/api --filter @studrc/web build",
    "lint": "pnpm -r --filter @studrc/api --filter @studrc/web lint",
    "db:generate": "pnpm --filter @studrc/api prisma:generate",
    "db:migrate": "pnpm --filter @studrc/api prisma:migrate",
    "db:studio": "pnpm --filter @studrc/api prisma:studio",
    "docker:dev": "docker compose -f docker-compose.dev.yml up -d",
    "docker:dev:down": "docker compose -f docker-compose.dev.yml down"
  },
  "engines": {
    "node": ">=20"
  }
}
